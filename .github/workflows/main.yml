name: Run Hello World

on:
  workflow_dispatch:

jobs:
  run-hello-world:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Hello World script
        run: |
          C:\Users\sandhata\Desktop\hello.bat
        shell: cmd
